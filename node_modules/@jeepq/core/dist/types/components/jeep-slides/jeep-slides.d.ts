/// <reference types="node" />
import { JSX, EventEmitter } from '../../stencil.core';
import { SlidesOptions, SlidesParams, SlidesMargins, HeaderVisibility } from '../../global/interfaces/jeep-slides';
export declare class JeepSlides {
    el: HTMLJeepSlidesElement;
    /**
     * The slides options
     */
    options: string;
    innerOptions: SlidesOptions;
    nSlides: number;
    fullScreen: boolean;
    parseOptionsProp(newValue: string): void;
    /**
     * Emitted the Header visibility change
     */
    onSlidesHeaderVisibility: EventEmitter<HeaderVisibility>;
    handleWindowResize(): void;
    handleJeepNavigationPrev(): Promise<void>;
    handleJeepNavigationNext(): Promise<void>;
    handleJeepPaginationIndex(event: CustomEvent): Promise<void>;
    handleJeepPlayControlsCurrentIndex(event: CustomEvent): Promise<void>;
    handleJeepFullscreenRequest(): Promise<void>;
    handleJeepFullscreenExit(): Promise<void>;
    handleJeepFullscreenChange(): Promise<void>;
    handleKeydown(event: KeyboardEvent): Promise<void>;
    handleTouchstart(): void;
    handleTouchmove(): void;
    handleTouchend(): Promise<void>;
    /**
     * Init data from properties.
     */
    init(): Promise<void>;
    /**
     * Set the slides.
     */
    setSlides(): Promise<void>;
    /**
     * Get Active Slide Index.
     */
    getActiveSlideIndex(): Promise<number>;
    /**
     *  Go Previous Slide
     */
    goToPreviousSlide(): Promise<void>;
    /**
     *  Go Next Slide
     */
    goToNextSlide(): Promise<void>;
    /**
     *  Go To Slide with given index
     */
    goToSlideIndex(index: number): Promise<void>;
    _element: any;
    _containerEl: HTMLElement;
    _wrapperEl: HTMLElement;
    _slidesEl: HTMLElement;
    _arrSlidesEl: Array<HTMLElement>;
    _slotEl: HTMLSlotElement;
    _navEl: HTMLJeepNavigationElement;
    _pagEl: HTMLJeepPaginationElement;
    _playEl: HTMLJeepPlayControlsElement;
    _fullEl: HTMLJeepFullscreenElement;
    _slidesParams: SlidesParams;
    _ticking: boolean;
    _direction: string;
    _navIcon: string;
    _initial: NodeJS.Timer;
    _elementsInSlot: JSX.Element[];
    _slidesNumber: number;
    _oriMargin: SlidesMargins;
    _touchStart: boolean;
    _touchMove: boolean;
    componentWillLoad(): Promise<void>;
    componentDidLoad(): Promise<void>;
    private _init;
    private _setSlides;
    private _setProperties;
    private _windowResize;
    private _getElementsInSlot;
    private _initScrollParams;
    private _doHide;
    private _doVisible;
    private _getCurrentSlideIndex;
    private _goPrev;
    private _goNext;
    private _setNavigationButtons;
    private _syncPagination;
    private _scrollToPosition;
    render(): any;
}

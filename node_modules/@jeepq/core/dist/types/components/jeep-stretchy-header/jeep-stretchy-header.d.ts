import { EventEmitter } from '../../stencil.core';
import { StretchyHeaderToolbar } from '../../global/interfaces/stretchyheader';
export declare class JeepStretchyHeader {
    el: HTMLJeepStretchyHeaderElement;
    /**
     * The Header Height
     */
    headerheight: string;
    /**
     * The Header Background Linear Gtradient if any and Url
     *  headerbackground="linear-gradient(to bottom, rgba(0,0,0,0), rgba(0,0,0,0.75)) ,
     *  url(https://raw.githubusercontent.com/jepiqueau/jeep/master/assets/images/italy-mountains.jpeg)"
     *
     */
    headerbackground: string;
    /**
     * The blur effect
     */
    headerbackgroundblur: boolean;
    /**
     * The Navbar Contrast Color
     */
    toolbarcontrastcolor: string;
    innerHeadHeight: string;
    innerUrl: string;
    innerGradient: string;
    innerBlur: boolean;
    innerContrastCol: string;
    parseHeaderHeight(newValue: string): Promise<void>;
    parseHeaderBackground(newValue: string): void;
    parseBlur(newValue: boolean): void;
    parseToolbarContrastColor(newValue: string): void;
    /**
     * Emitted the Header visibility change
     */
    onStretchyHeaderToolbar: EventEmitter<StretchyHeaderToolbar>;
    /**
     * Init data from properties.
     */
    init(): Promise<void>;
    private _window;
    private _document;
    private _root;
    private _header;
    private _mouseStart;
    private _ptStart;
    private _headerBackground;
    private _ionHeader?;
    private _ionToolbar?;
    private _ionContent?;
    private _heightIni;
    private _height;
    private _minHeight;
    private _isScroll;
    private _scrollTop;
    private _prevScrollTop;
    private _ionColor;
    private _ionColorContrast;
    componentWillLoad(): Promise<void>;
    componentDidLoad(): Promise<void>;
    private _init;
    private _setHeader;
    private _setToolbar;
    private _setContent;
    private _setTranslucent;
    private _handleStart;
    private _handleMove;
    private _handleEnd;
    render(): any;
}
